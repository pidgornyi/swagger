parameters:

  value:
    in: query
    name: value
    type: string
    description: free text criteria

  itemsCount:
    in: query
    name: itemsCount
    description: limits number of search results
    type: integer
    format: int32
    default: 20

